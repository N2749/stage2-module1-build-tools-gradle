//add everything here

dependencies {
    testImplementation('com.mjcschool:utils:1.3.5')
}

repositories {
    mavenLocal()
    mavenCentral()
}

test {
    useJUnitPlatform()
}

compileJava {}.dependsOn(':utils:publishToMavenLocal')